# StudX

Автоматизация академических работ на основе промптов и протоколов

## 🔥 ЖЕЛЕЗНЫЕ ПРАВИЛА АРХИТЕКТУРЫ (ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ!)

### 🚨 АНТИ-МОНСТР ПРОТОКОЛ: 50-100 строк или смерть!

**ПРЕВЕНТИВНЫЕ ТРИГГЕРЫ (контролирую В ПРОЦЕССЕ написания):**
- **Файл > 80 строк** → СТОП → планирую разбиение  
- **Функция > 30 строк** → выношу в отдельный файл
- **Компонент > 3 условных рендера** → разбиваю на подкомпоненты
- **API файл > 5 endpoints** → разбиваю по сущностям
- **Копипащу код 3-й раз** → выношу в общую функцию
- **Вложенность > 3 уровней** → рефакторю структуру
- **Импортов > 10 в файле** → признак монстра, разбиваю
- **Параметров > 4 в функции** → использую объект
- **Комментарий "это сложно"** → сигнал к декомпозиции

**НЕ ЖДУ SONARQUBE - РЕШАЮ СРАЗУ!**


#### 1️⃣ **50-100 строк максимум на файл** - БЕЗ ИСКЛЮЧЕНИЙ
- Файл приближается к 80 строкам → автоматическая декомпозиция
- Функция > 30 строк → выносится в отдельный модуль
- CSS файлы тоже компактные (до 150 строк)

#### 2️⃣ **Один файл = одна ответственность** - ЖЕЛЕЗНО
- OrderCard только рендерит карточку, не управляет данными
- OrderForm только форма, валидация в отдельном файле
- API client только отправляет запросы, не форматирует данные

#### 3️⃣ **README.md в каждом модуле** - ОБЯЗАТЕЛЬНО
```
ComponentName/
├── index.jsx      (50-100 строк)
├── styles.css     (компактные стили)
└── README.md      (что делает, как использовать)
```

#### 4️⃣ **Структура папок предсказуемая** - ВСЕГДА
```
layouts/     → обертки страниц
pages/       → страницы-контейнеры  
components/  → переиспользуемые компоненты
services/    → API и бизнес-логика
utils/       → чистые функции
hooks/       → кастомные React хуки
mocks/       → данные для разработки
```

#### 5️⃣ **Композиция вместо God-компонентов** - БЕЗ ВАРИАНТОВ

**НЕ ТАК (God-компонент):**
```javascript
function Card({type, data}) {
  if(type === 'user') {/* 100 строк */}
  if(type === 'order') {/* 100 строк */}
  if(type === 'product') {/* 100 строк */}
}
```

**ПРАВИЛЬНО (композиция):**
```javascript
const cardMap = {user: UserCard, order: OrderCard, product: ProductCard};
const Card = ({type, ...props}) => {
  const Component = cardMap[type];
  return <Component {...props}/>;
}
```

### Почему это КРИТИЧЕСКИ важно:

**Опыт создания админ-панели показал:**
- **38 чистых файлов** вместо 5 файлов-монстров = код читается за минуту
- **Дебаг за 5 минут** вместо часа поиска в файле на 1000 строк
- **Изменения без страха** - трогаешь 50 строк, не боишься сломать все
- **Параллельная работа** - каждый работает над своим модулем

---

## 🛡️ ЗАЩИТНЫЕ МЕХАНИЗМЫ АРХИТЕКТУРЫ

### 📌 Git как источник истины - КОММИТ ПОСЛЕ КАЖДОГО БЛОКА!

**ЖЕЛЕЗНОЕ ПРАВИЛО версионирования:**
- ✅ Реализована функция → коммит
- ✅ Исправлен баг → коммит  
- ✅ Завершен рефакторинг → коммит
- ✅ Обновлена конфигурация → коммит
- ✅ Изменены зависимости → коммит

**Структура коммитов:**
```
feat: новая функциональность
fix: исправление багов
refactor: улучшение без изменения логики
chore: обновление зависимостей
docs: документация
```

**БЕЗ коммитов = потеря контекста = переделка работы!**

### 🎯 Самоорганизующиеся модули

Каждый модуль САМ регистрирует себя:

```javascript
// modules/feature/index.js
export default {
  name: 'Feature',
  register: (app) => {
    app.addRoute('/feature', FeatureComponent);
    app.addMenuItem('Feature', '/feature');
    app.addAPI('/api/feature', featureAPI);
  }
}
```

**Добавление модуля = копирование папки + изменение 3 строк**

### 🔒 Контракты между слоями

- **Frontend → Backend:** Только через /api/* endpoints
- **Backend → Database:** Только через ORM/query builder  
- **Модули → Ядро:** Только через register() интерфейс
- **Компоненты → Данные:** Только через hooks

**НАРУШЕНИЕ ГРАНИЦ = ТЕХДОЛГ = РЕФАКТОРИНГ**

### 💡 Золотые правила индустрии

**DRY с умом:** Правило трех - дублирование допустимо 2 раза, на 3-й → выносим в модуль

**YAGNI:** Не делай на будущее - обобщаем только на ВТОРОМ реальном кейсе

**Fail Fast:** Валидация на входе, явные ошибки, ранний return

**Принцип наименьшего удивления:** getUserById() возвращает user, не массив. Имя = контракт.

**Dependency Injection:**
```javascript
// НЕ ТАК:
function processOrder() {
  const db = new PostgreSQL(); // Хардкод
}

// ПРАВИЛЬНО:
function processOrder(db) { // Инжектим
  // используем переданную БД
}
```


---

## 🧠 Ядро системы

**Центральная оркестровка:** Вся бизнес-логика генерации управляется через **Claude API** - это мозг системы, который понимает контекст, применяет промпты и координирует весь процесс создания работ.

**Дополнительные AI:** Perplexity API для исследований и поиска информации на определенных этапах генерации.

**Уникальность:** В основе системы - проверенные промпты, протоколы и последовательности действий, отработанные при ручном выполнении заказов на Studwork. Это не просто генератор, а автоматизация реального опыта создания качественных академических работ.

## 🎯 MVP Стратегия

**MVP = стабильная работа для одного пользователя (владельца)**

Сначала делаем полнофункциональную систему для внутреннего использования. Когда все процессы будут отлажены и стабильны - можно масштабировать на клиентов.

## 👥 Два типа кабинетов

1. **Админ-панель (текущий фокус)** - полный контроль над системой, диагностика, управление заказами
2. **Клиентский кабинет (будущее)** - упрощенный интерфейс для заказчиков

## 📋 Основные возможности

- Генерация академических работ различных типов
- Автоматическое форматирование по ГОСТ
- Работа с шаблонами документов
- API интеграции для генерации контента
- Модульная архитектура для расширения

## 🛠 Технологический стек

- **Frontend:** React 18.3 (CRA, React Router 6, Axios с retry)
- **Backend:** Python (FastAPI для API, обработка через Claude)
- **База данных:** PostgreSQL (схема studx - 8 таблиц)
- **AI оркестровка:** Claude API + Perplexity API
- **Инфраструктура:** VPS на DigitalOcean

## 📁 Структура проекта

```
StudX/
├── backend/           # Серверная часть
│   ├── src/          # Исходный код
│   ├── config/       # Конфигурация
│   └── tests/        # Тесты
├── frontend/         # Клиентская часть
│   ├── src/          # Исходный код React
│   └── public/       # Статические файлы
└── docs/             # Документация
```
## 🚀 Установка и запуск

### Требования
- Node.js 18+ 
- PostgreSQL 14+
- npm или yarn

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/ViaVisAI/StudX.git
cd StudX

# Установка зависимостей backend
cd backend
npm install

# Установка зависимостей frontend
cd ../frontend
npm install
```

### Запуск в режиме разработки

```bash
# Backend
cd backend
npm run dev

# Frontend (в новом терминале)
cd frontend
npm run dev
```

## 📝 Документация

Подробная документация находится в папке `/docs`

## 🤝 Контрибьюция

Приветствуются пулл-реквесты. Для больших изменений сначала откройте issue.

## 📄 Лицензия

MIT

## 👥 Команда

Проект разрабатывается командой ViaVisAI

---

*Актуальная версия документации всегда находится в репозитории*# Test comment for Quality Gates check
# AI-Driven Development test